# NVIDIA Isaac Sim 5.1.0 安装与使用指南

> **文档版本**: v1.0
> **适用平台**: NVIDIA DGX Spark (aarch64, Ubuntu 24.04)
> **Isaac Sim 版本**: 5.1.0 (Headless Mode)
> **最后更新**: 2026-02-04

---

## 📋 目录

1.  [概述](#一-概述)
2.  [安装步骤](#二-安装步骤)
3.  [验证安装](#三-验证安装)
4.  [使用 Isaac Sim 容器](#四-使用-isaac-sim-容器)
5.  [Isaac Lab 集成](#五-isaac-lab-集成)
6.  [故障排查](#六-故障排查)

---

## 一、概述

本指南详细介绍了如何在 NVIDIA DGX Spark 上通过 Docker 容器安装和配置 Isaac Sim 5.1.0。考虑到 DGX Spark 是一个服务器环境，我们选择无头模式 (Headless Mode) 安装，以支持后台仿真和脚本自动化。

**Isaac Sim 核心优势:**
-   **物理精确仿真**: 基于 NVIDIA Omniverse，提供高精度的物理仿真。
-   **GPU 加速**: 利用 DGX Spark 的强大 GPU 性能，实现更快的仿真速度。
-   **ROS2 集成**: 通过 `--network host` 模式，方便与主机上的 ROS2 环境通信。

---

## 二、安装步骤

### 2.1 脚本准备

安装将通过自动化脚本 `install_isaac_sim_docker.sh` 完成。该脚本已上传到您的 DGX Spark 主目录。

### 2.2 执行安装

请在您的本地机器上执行以下命令，SSH 到 DGX Spark 并运行安装脚本。

```bash
# 1. SSH 到 DGX Spark
ssh spark

# 2. 运行 Isaac Sim 安装脚本
# 脚本会安装 NVIDIA Container Toolkit，拉取 Docker 镜像，并启动 Isaac Sim 容器。
# 过程中可能会提示输入 sudo 密码：230000IceFlame
bash ~/install_isaac_sim_docker.sh
```

**脚本主要操作:**
-   检查并安装 NVIDIA Container Toolkit (解决 `gpg` 错误)
-   确保 `leo` 用户在 `docker` 组中 (请重新登录以使权限生效)
-   创建 `/home/leo/isaac_sim_data` 和 `/home/leo/isaac_sim_cache` 用于持久化数据
-   拉取 `nvcr.io/nvidia/isaac-sim:5.1.0` Docker 镜像
-   启动 `isaac-sim-5.1.0-headless` 容器 (在后台运行)
-   创建 `~/isaac_sim_bash.sh` 辅助脚本，用于快速进入容器

---

## 三、验证安装

### 3.1 验证 Docker 容器状态

安装脚本执行完成后，请检查 Isaac Sim 容器是否正在运行：

```bash
# 在 DGX Spark 终端执行
ssh spark "docker ps | grep 'isaac-sim-5.1.0-headless'"
```
**预期输出**: 应该显示一个容器 ID、镜像名称 (`nvcr.io/nvidia/isaac-sim:5.1.0`) 和其运行状态 (`Up ...`)。

### 3.2 验证辅助脚本

确认进入容器的辅助脚本是否存在：

```bash
# 在 DGX Spark 终端执行
ssh spark "ls -l ~/isaac_sim_bash.sh"
```
**预期输出**: 显示 `isaac_sim_bash.sh` 脚本的文件信息和可执行权限。

### 3.3 运行 Isaac Sim 示例 (Headless Mode)

1.  **进入 Isaac Sim 容器**:
    ```bash
    # 在 DGX Spark 终端执行
    ~/isaac_sim_bash.sh
    ```
2.  **在容器内运行示例**:
    ```bash
    # 在容器内部终端执行
    cd /isaac-sim/standalone_examples/api/omni.isaac.gym/
    python cartpole.py --headless
    ```
    *   **预期结果**: 示例脚本将执行，并打印仿真相关日志。不会有图形界面弹出。
    *   **退出容器**: 输入 `exit`。

---

## 四、使用 Isaac Sim 容器

### 4.1 启动 / 停止容器

-   **启动 (如果已停止)**:
    ```bash
    docker start isaac-sim-5.1.0-headless
    ```
-   **停止**:
    ```bash
    docker stop isaac-sim-5.1.0-headless
    ```
-   **移除容器**:
    ```bash
    docker rm isaac-sim-5.1.0-headless
    ```

### 4.2 访问容器文件系统

-   容器启动时，主机目录 `~/projects` 被挂载到容器内部的 `/workspace/projects`。
-   您可以在主机上修改 `~/projects/XR-1` 的代码，容器内部会自动同步。
-   Isaac Sim 的安装目录在容器内部是 `/isaac-sim`。

### 4.3 ROS2 集成

-   容器以 `--network host` 模式运行，这意味着容器内的 ROS2 节点可以直接与主机上的 ROS2 节点（如 `ros2_humble` 环境）通信。
-   确保在主机和容器内都设置正确的 `ROS_DOMAIN_ID` (脚本已配置为 `0`)。
-   在容器内，ROS2 环境通常已经配置好。您可以直接运行 `ros2 topic list` 等命令。

---

## 五、Isaac Lab 集成

Isaac Lab 是 Isaac Sim 的强化学习框架，通常作为 Isaac Sim 的扩展安装或在 Isaac Sim 环境内部构建。

1.  **进入 Isaac Sim 容器**:
    ```bash
    ~/isaac_sim_bash.sh
    ```
2.  **在容器内安装 Isaac Lab**:
    *   **请参考 Isaac Lab 官方文档**，因为其安装步骤可能因版本而异。
    *   通常涉及克隆 Isaac Lab 仓库，并在 Isaac Sim 提供的 Python 环境中安装依赖。
    *   **Isaac Lab 文档**: [https://isaac-sim.github.io/IsaacLab/main/source/setup/installation/index.html](https://isaac-sim.github.io/IsaacLab/main/source/setup/installation/index.html)
3.  **运行 Isaac Lab 示例**:
    ```bash
    # 通常在容器内执行
    cd /isaac-sim/apps/omni.isaac.lab_tasks/
    python main.py --task Cartpole --headless
    ```

---

## 六、故障排查

### 6.1 容器启动失败 / 立即退出

-   **问题**: `docker ps -a` 显示容器 `Exited` 或根本不存在。
-   **解决**:
    1.  **查看容器日志**: `docker logs isaac-sim-5.1.0-headless`，找出启动失败的具体原因。
    2.  **检查 GPU**: 确保 GPU 驱动正常 `nvidia-smi`。
    3.  **检查 NVIDIA Container Toolkit**: 确保 `docker info | grep "Runtimes: nvidia"` 显示 `nvidia`。

### 6.2 `leo` 用户无 `docker` 权限

-   **问题**: 运行 `docker` 命令时提示权限错误 (Permission Denied)。
-   **解决**:
    1.  确认 `leo` 已添加到 `docker` 组：`grep docker /etc/group`。
    2.  **完全注销并重新登录 SSH 会话**，以使组权限生效。
    3.  如果无法重新登录，可以尝试 `newgrp docker` (但在某些情况下可能无法完全生效)。

### 6.3 网络问题

-   **问题**: 容器内无法访问外部网络，或 ROS2 无法通信。
-   **解决**:
    1.  确保容器以 `--network host` 模式运行。
    2.  检查主机 DGX Spark 的网络连接。
    3.  确认 ROS2 环境变量 `ROS_MASTER_URI` 和 `ROS_IP` 在容器和主机上都已正确配置 (通常在容器启动时通过 `-e` 传递)。

---

## 📚 参考文档

-   **Isaac Sim 官方文档**: [https://docs.isaacsim.omniverse.nvidia.com/](https://docs.isaacsim.omniverse.nvidia.com/)
-   **Isaac Lab 官方文档**: [https://isaac-sim.github.io/IsaacLab/](https://isaac-sim.github.io/IsaacLab/)
-   **DGX Spark 文档**: [https://build.nvidia.com/spark](https://build.nvidia.com/spark)

---

**文档维护**: DGX Spark开发环境
**配置完成时间**: 2026-02-04
